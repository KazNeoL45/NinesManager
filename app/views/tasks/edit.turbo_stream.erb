<%= turbo_stream.remove "taskModal" %>

<%= turbo_stream.append "modals", partial: "tasks/edit_modal", locals: { task: @task, project: @project } %>
<%= turbo_stream.action :show_modal, target: "editTaskModal" %>